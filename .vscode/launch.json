{
    "version": "2.0.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "gdb",
            "request": "launch",
            "target": "./debug/main",
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "build"
        }
    ]
}